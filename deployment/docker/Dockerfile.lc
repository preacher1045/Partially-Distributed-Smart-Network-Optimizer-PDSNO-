FROM python:3.10-slim

# Install ping utility required by ICMP discovery scanner.
RUN apt-get update && apt-get install -y --no-install-recommends \
    iputils-ping \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /pdsno